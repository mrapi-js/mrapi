{
  "name": "dal-basic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only ./src/app.ts",
    "prod": "cross-env NODE_ENV=production node dist/app",
    "build": "tsc",
    "clean": "rm -rf dist",
    "generate": "npm -s run generate:prisma && npm -s run generate:nexus",
    "generate:prisma": "npx prisma-multi-tenant generate",
    "generate:nexus": "ts-node-dev --transpile-only src/nexus-schema",
    "list": "prisma-multi-tenant list"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mrapi/dal": "workspace:*",
    "graphql": "15.3.0",
    "@types/express": "^4.17.7",
    "express": "^4.17.1",
    "express-graphql": "^0.11.0",
    "cross-env": "^7.0.2",
    "@nexus/schema": "0.15.0",
    "nexus-plugin-prisma": "0.17.0",
    "@prisma/client": "2.4.1",
    "@prisma/cli": "2.4.1",
    "prisma-multi-tenant": "2.2.0",
    "@prisma-multi-tenant/client": "2.2.0",
    "@prisma-multi-tenant/shared": "2.2.0"
  },
  "devDependencies": {
    "typescript": "^3.9.6",
    "ts-node-dev": "1.0.0-pre.51"
  },
  "prettier": {
    "semi": false,
    "tabWidth": 2,
    "singleQuote": true,
    "trailingComma": "all"
  }
}
