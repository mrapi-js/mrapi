model User {
  email String  @unique
  id    Int     @default(autoincrement()) @id
  name  String?
  test Test[]
  nn TTT []
  yy DDD[]
}

model Test {
  id Int @id
}

enum TTT {
  XXX
}

enum DDD {
  YYY
}