model Tenant {
  id        Int      @id @default(autoincrement())
  name      String
  service   String
  database  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name, service])
}
